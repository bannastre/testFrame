document.addEventListener('DOMContentLoaded', function() {

  var parent = document.getElementById('tests');
  var child = document.createElement('li');
  child.innerHTML = "get tests to populate here"
  parent.appendChild(child);

});
